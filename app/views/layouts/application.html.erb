<!DOCTYPE html>
<html>
  <head>
    <title>Freela</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <% if alert %>
    <div class="bg-red-100 border border-red-400 text-xs text-red-700 mx-2 my-2 px-2 py-3 rounded relative">
      <%= alert %>
    </div>
    <% end %>
    
    <% if notice %>
    <div class="bg-green-100 border border-green-400 text-xs text-green-700 mx-2 my-2 px-2 py-3 rounded relative">
      <%= notice %>
    </div>
    <% end %>
    <%= yield %>
  </body>
</html>
