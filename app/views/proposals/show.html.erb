<h2>Interesse no projeto: <%= @proposal.project.title %> </h2>
<p><%= @proposal.resume %><p>

<h2>Valor da hora trabalhada: </h2>
<p><%= number_to_currency @proposal.payment_hour %><p>

<h2>Horas dedicadas na semana: </h2>
<p><%= @proposal.week_hours %><p>

<h2>Data prevista para conclusão:</h2>
<p><%= l @proposal.conclusion_date %><p>
<h2>Dono do projeto: <%= @proposal.project_owner.email %> </h2>

<p class="text-gray-800 text-sm font-medium my-2 text-justify">
      <b>Status: </b>
      <% if @proposal.pending? %>
        <span class="bg-yellow-100 text-yellow-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded-md"><%= t @proposal.status %></span>
      <% elsif @proposal.accepted? %>
        <span class="bg-green-100 text-green-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded-md"><%= t @proposal.status %></span>
      <% elsif @proposal.rejected? %>
        <span class="bg-red-100 text-red-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded-md"><%= t @proposal.status %></span> 
      <% end %>
    </p>